{
  "name": "example-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://4saph.github.io/to-do-app_react",
  "devDependencies:" {
    "gh-pages": "^1.2.0",
    "react-scripts": "1.1.4",
  }
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
  },
  "scripts": {
    "start": "react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
